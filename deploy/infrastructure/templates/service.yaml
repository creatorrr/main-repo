apiVersion: v1
kind: Service
metadata:
  name: {{ template "infrastructure.fullname" . }}
  labels:
    app: {{ template "infrastructure.name" . }}
    chart: {{ template "infrastructure.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "infrastructure.name" . }}
    release: {{ .Release.Name }}
